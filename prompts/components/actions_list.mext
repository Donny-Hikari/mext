The following actions are available.
{@for action in action_list}
- name: {action[name]}
  desc: {action[desc]}
  parameters: {@if not empty action[parameters]}
    {@for param in action[parameters].items}
    {param[0]}: {param[1]}
    {@endfor}
  {@else}[]
  {@endif}
{@endfor}